import { base } from '$app/paths';

const dbBase = `${base}/sds`;

export const SDS_HOME_URL = `${base}/sds/home`;
export const SDS_LOGIN_URL = `${dbBase}/login`;
export const SDS_BASE = `${dbBase}`;
export const SDS_AUTO_LOGIN_URL = `${dbBase}/login?auto=1`;
export const SDS_BASE_URL = `${dbBase}/`;
// export const SDS_COOKIE_PATH = `${dbBase}`;
// export const SDB_DATABASE = 'sdb';
// export const SDB_PASSWORD_FILE = '/var/www/sds/sdb-passwd-apache-without-dbname';
// export const SDS_QUERY_LOG = '/var/www/sds/log/querylogs/sds';
